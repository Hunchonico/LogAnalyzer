LogAnalyzer is a small but production-style Python project that turns raw log files into actionable summaries. It supports analyzing a single log file or a whole folder, extracts best-effort fields, and produces three report formats: report.json (machine-readable), report.md, and report.html. The project is structured as a proper package, includes unit tests, and runs automatically in GitHub Actions.